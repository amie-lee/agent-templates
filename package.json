{
  "name": "agent-init",
  "version": "1.0.0",
  "description": "Scaffold a multi-agent development project. Generates PLAN.md and agent prompt files.",
  "main": "agent-init.js",
  "bin": {
    "agent-init": "./agent-init.js"
  },
  "scripts": {
    "test": "node smoke-test.js"
  },
  "keywords": ["agent", "ai", "scaffold", "project-init", "multi-agent"],
  "license": "MIT",
  "engines": {
    "node": ">=16"
  }
}
